export const defaultConfig = {
  width: 600,
  height: 600,

  particles: 1200,

  minSize: 0.4,
  maxSize: 2.3,
  minSpeed: 0.00004,
  maxSpeed: 0.00012,
};

export type Config = typeof defaultConfig;
