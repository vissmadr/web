# Developer Documentation

This directory is reserved for human-only developer documentation, notes, drafts, and private context.

AI assistants must skip this directory completely. Do not read, summarize, edit, move, delete, or use files in this directory unless the user explicitly overrides that rule for a specific file and task.
